const DEFAULT_LOCAL_STORAGE_PREFIX = "thesisflow";

export const STORAGE_CONFIG = {
  authLocalStorageKey: `${DEFAULT_LOCAL_STORAGE_PREFIX}-auth`,
  userLocalStorageKey: `${DEFAULT_LOCAL_STORAGE_PREFIX}-user`,
  i18nLocalStorageKey: `${DEFAULT_LOCAL_STORAGE_PREFIX}-i18n`,
};
